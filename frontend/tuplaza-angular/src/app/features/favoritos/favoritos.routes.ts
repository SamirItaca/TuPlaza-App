import { Routes } from '@angular/router';
import { FavoritosComponent } from './pages/favoritos.component';

export const FAVORITOS_ROUTES: Routes = [
  { path: '', component: FavoritosComponent },
];
