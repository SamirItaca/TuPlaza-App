import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuplaza.app',
  appName: 'tuplaza-angular',
  webDir: 'dist/tuplaza-angular/browser'
};

export default config;
